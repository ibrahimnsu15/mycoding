#include <iostream>
#include <map>
#define FORIT(i,m) for(_typeof((m).begin()) i = (m).begin() ; i != (m).end() ; i++)
using namespace std;

int main(){
    map<string,int> m;

    m["ibrahim"] = 21;
    m["murad"] = 12;

    cout<<m.size()<<endl;

    cout<<m["ibrahim"]<<endl;


    return 0;
}
